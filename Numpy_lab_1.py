#Convert the below list into numpy array then display the array

import numpy as np

my_list = [1, 2, 3, 4, 5]
arr = np.array(my_list)
print(arr)


#Convert the below list into a numpy array

import numpy as np

my_list = [1, 2, 3, 4, 5]
arr = np.array(my_list)
print(arr)
print(arr[0], arr[-1])
print(arr * 2)